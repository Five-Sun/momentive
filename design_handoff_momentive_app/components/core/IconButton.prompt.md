Circular icon button — the wishlist heart on every product card, back/close controls, the filter toggle.

```jsx
<IconButton active={isFavorited} onClick={toggleFavorite}>♥</IconButton>
```

`filled` variant floats over photography (product image overlays) with the system's one card shadow; `outline` sits on plain surfaces (headers, toolbars).
