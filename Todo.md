# Todo

세션이 바뀌어도 다음 작업을 판단할 수 있도록 남은 일만 관리한다. 상세한 결정과 검증 근거는 `docs/specs/`, `docs/plans/`, `docs/backlog/`, `docs/e2e/` 및 PR 본문이 원천이다.

## 사용법

- 새 기능은 작업 전에 반드시 grillme → spec → plan 순서로 확정한다. 이 파일의 항목은 구현 지시가 아니라 우선순위 후보이다.
- 한 항목(또는 같은 spec으로 묶인 작은 항목)을 끝내면 검증 결과와 관련 문서를 갱신한 뒤 체크한다.
- **출시 게이트**는 아래의 작은 개선과 무관하게, 실제 고객 오픈 전에 반드시 모두 끝낸다.
- 현재 기본 브랜치는 `develop`이다. 기능 하나의 spec/plan·구현·이 파일 반영은 하나의 PR로 묶는다.

---

## 출시 게이트 — 고객 오픈 전 필수

**진행 순서: G2 → G1 → G3.** 원래 G1부터 순서대로 착수했으나, Toss 결제위젯("주문서형·결제창형 연동") 이용 신청 화면이 **필수 항목으로 운영 홈페이지 URL**을 요구한다는 걸 2026-09-15에 확인했다. G3도 정의상 "운영 배포 직후" 항목이라 이미 G2에 의존하고 있었으므로, G2(배포)를 먼저 끝내고 그 URL로 G1을 신청 → G3을 잇는 순서로 조정한다.

### G1. 토스페이먼츠 실결제 개통 및 전체 흐름 검증

- [ ] Toss 개발자센터에서 "주문서형·결제창형 연동"(구 결제위젯) 이용 신청 및 심사를 완료한다. 사업자등록번호 입력 필요, **운영 홈페이지 URL 필수 입력**이라 G2 완료 후 진행. **사용자 외부 조치 필요**
- [ ] 샌드박스에서 결제위젯 렌더링 → 결제수단 선택 → 승인 성공 → `PAID` 주문/주문완료 화면까지 실제로 검증한다.
- [ ] 실제 `PAID` 주문을 고객 및 관리자가 취소했을 때 토스 취소/환불, 재고 복원, 쿠폰 복원이 모두 정확한지 검증한다.
- [ ] 부분 결제 후 결제한 장바구니 항목만 제거되는지 브라우저에서 검증한다.

완료 기준: `docs/e2e/2026-08-29-cart-order-payment.md` 시나리오 7 및 관련 보류 항목이 실제 Toss 샌드박스에서 PASS한다.

근거: `docs/backlog/2026-08-30-cart-order-payment-phase4-01.md`, `docs/specs/2026-08-29-cart-order-payment.md`. 2026-08-30 당시엔 "상점 미등록"으로 추정했으나, 2026-09-15 재확인 결과 체험 상점과 API 개별 연동 키는 이미 있고, 결제위젯(주문서형·결제창형 연동) 자체의 "이용 신청"만 안 되어 있었다 — 신청 폼이 운영 홈페이지 URL을 필수로 요구해 G2 완료 후 재개.

### G2. 운영 배포 구성 및 스모크 테스트

- [ ] Railway 백엔드 환경변수(DB, JWT, Toss, Cloudinary, CORS)를 설정하고 `api.<도메인>` HTTPS API가 정상 응답하는지 확인한다.
- [ ] Vercel 프론트 환경변수와 운영 API URL을 설정하고, 로그인·상품 조회·이미지 업로드·주문 생성이 운영 도메인에서 되는지 확인한다.
- [ ] 운영 DB에 Flyway 마이그레이션이 정상 적용됐는지, 오류 로그와 헬스체크를 확인한다.
- [ ] 관리자 계정을 운영 DB에서 수동 승격하고 `/admin` 접근/일반 회원 403을 확인한다.

완료 기준: 실제 운영 URL에서 고객 1개 계정과 관리자 1개 계정으로 핵심 흐름을 재현하고, 시크릿이 프론트/로그에 노출되지 않는다.

**알려진 이슈 (2026-09-16)**: 현재 프론트(`momentive-nine.vercel.app`)와 백엔드(`momentive-production.up.railway.app`)가 서로 다른 도메인이라, 백엔드가 발급하는 host-only 인증 쿠키(`SameSite=Lax`)를 프론트 서버(Vercel)가 서버사이드에서 읽지 못해 `/admin` 진입 시 DB role이 ADMIN이어도 비로그인으로 판정된다. 임시 조치로 인증 쿠키를 `SameSite=None; Secure`로 바꾸고 `/admin` 게이트를 서버사이드 대신 클라이언트에서 `credentials: 'include'`로 확인하는 방식으로 전환 중. **커스텀 도메인(`api.<도메인>` 서브도메인) 적용 시 이 임시 조치를 되돌리고 서버사이드 쿠키 인증 방식으로 정리할 것** — G1의 운영 도메인 확보 시점에 함께 처리.

### G3. 기존 데이터 마이그레이션 확인

- [ ] 운영 배포 직후 기존 `PAID` 주문의 `V17` 배송상태가 `PREPARING`으로 백필됐는지 SQL로 확인한다.
- [ ] 마이그레이션 이전 주문의 상품명·사이즈·금액이 고객 주문 목록/상세에서 정상 표시되는지 확인한다.

완료 기준: 실제 운영의 기존 주문 표본으로 두 항목을 확인한다. 로컬 DB를 초기화해 백필 대상이 없어 로컬에서는 검증하지 못했다.

---

## 실행 대기열 — 작은 문제부터

### 1. 즉시 수정 후보 (작고 독립적)

| 순서 | 항목 | 유형 | 완료 기준 | 참고 |
|---:|---|---|---|---|
| 1 | 상품 조회 실패 화면 | 오류 처리 | 홈·검색·위시리스트 API 실패 시 스켈레톤에 멈추지 않고 재시도 가능한 안내가 보인다 | 기존 4단계 항목 |
| 2 | 인기순 정렬을 실제 인기순으로 | 기능 정확성 | 검색 `POPULAR`과 홈 인기 섹션이 합의한 실제 지표(리뷰 수 등)로 정렬되고 라벨과 일치한다 | 기존 4단계 항목 |
| 3 | 장바구니 수량 재고 상한 | 구매 버그 | 재고보다 많이 담거나 수량을 올릴 수 없고, 변동 재고는 서버에서도 거부한다 | 기존 4단계 항목 |
| 4 | 상품 상세 배송비 안내 보강 | 안내 정확성 | 기본 배송비 3,400원·7만원 이상 무료·제주/도서산간 +4,000원이 정책과 일치한다 | 제주 금액만 수정, 나머지는 보류 |

각 행은 독립된 기능 변경이므로 착수 시 grillme로 범위가 충분히 작은지 확인하고, 필요하면 같은 성격의 항목만 하나의 spec으로 묶는다.

### 2. 구매 경험 보완

| 순서 | 항목 | 완료 기준 | 선행/주의 |
|---:|---|---|---|
| 5 | 장바구니·주문서·주문상세 썸네일 | 세 화면에서 실제 상품 이미지를 일관되게 표시한다 | Cart/Order API 응답·타입 확장 필요 |
| 6 | 주문내역 페이지네이션 | 많은 주문에서도 목록이 페이지 단위로 조회되고 이동/빈 상태가 정상이다 | 백엔드 목록 API와 함께 설계 |
| 7 | 장바구니 가격 최신화 | 가격 변경 뒤 주문 생성 시 서버 가격을 기준으로 재계산·안내한다 | 결제금액 무결성에 영향, G1 전 검증 권장 |
| 8 | 홈 프로모션 배너 | 실제 행사/상품 목록으로 연결되거나, 운영하지 않는 배너는 제거한다 | 사업 운영 결정 필요 |

### 3. 운영 기능·검증 사각지대

| 순서 | 항목 | 완료 기준 | 비고 |
|---:|---|---|---|
| 9 | 관리자 주문 목록 페이지네이션 검증 | 여러 페이지·필터 조합과 일반 회원 접근 거부를 확인한다 | 로컬 주문 1건으로 미검증 |
| 10 | 쿠폰 발급 운영 경로 | 관리자가 쿠폰을 발급/대상 지정할 수 있고 만료 쿠폰도 회귀 검증에 쓴다 | 현재는 Flyway 시드만 발급 경로; 별도 spec 필요 |
| 11 | 쿠폰 미검증 AC 해소 | 할인 상한·서버 재검증·실패/만료/취소 복원이 자동/수동 검증으로 모두 확인된다 | 현 상태 AC 13/20 |
| 12 | 우편번호 형식 검증 | 저장/주문 주소에 합의한 형식 검증과 기존 데이터 호환 전략이 적용된다 | 기존 데이터 영향 검토 필요 |
| 13 | 리뷰 신고/모더레이션 여부 결정 | 운영 정책과 필요 시 관리 화면 범위를 문서화한다 | 현재 미지원 |

### 4. 사업 콘텐츠·후순위 결정

- [ ] 실상품 사진과 실제 판매 상품/재고를 등록한다. 현재 시드 이미지는 `picsum.photos`이며 카테고리도 ACCESSORY에 치우쳐 있다.
- [ ] 인기 검색어를 집계 기반으로 만들지, 제거할지 결정한다. 현재는 하드코딩이다.
- [ ] 적립금 제도를 도입할지 결정한다. 현재 UI와 백엔드 도메인은 없으며, 도입 시 별도 spec 대상이다.
- [ ] 택배사 실시간 배송조회 연동 필요성을 결정한다. 현재는 관리자가 입력한 택배사·송장번호·상태를 보여주는 방식이다.

---

## 현재 완료된 기반

아래는 재구현 대상이 아니라 후속 작업의 전제다.

- 고객: 인증, 상품 카탈로그/검색, 찜, 리뷰, 장바구니, 주소, 주문 생성, 배송비, 쿠폰, 주문내역·배송상태 조회
- 관리자: 역할 기반 접근, 상품/variant/재고 CRUD, Cloudinary 서명 업로드, 주문 목록·상세, 배송상태/송장 수정, 주문 취소
- 로그인 하이드레이션 전 제출 방지 (PR #20)
- 최근 완료: 배송조회 (`docs/specs/2026-09-09-delivery-tracking.md`, PR #21)

## 알려진 환경 메모

- `./dev.sh`가 DB·백엔드·프론트를 함께 기동한다. 로그는 `.dev-logs/backend.log`, `.dev-logs/frontend.log`.
- 로컬 DB가 비어 있는데 Flyway 이력만 성공 상태면 `docker compose down -v` 후 재기동으로 재시딩한다. 이 명령은 로컬 DB 데이터를 지우므로 검증 데이터가 필요하면 먼저 보존한다.
- 테스트가 개발 DB를 공유해 데이터를 지울 수 있다. E2E 검증 데이터와 `./gradlew test` 실행 순서를 분리한다.
- 관리자 승격은 환경별로 기존 사용자에 DB 수동 `UPDATE` 후 재로그인이 필요하다.
